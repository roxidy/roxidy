[project]
name = "qbit-cli-tests"
version = "0.1.0"
description = "Integration tests for qbit-cli"
requires-python = ">=3.10"
dependencies = [
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
